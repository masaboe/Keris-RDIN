# Stage 01 - Background removal (rembg) -> PNG RGB with white background
seed: 42

data:
  input_dir: data/raw_images
  output_dir: data/no_background

params:
  alpha_thr: 1
  morph_ksize: 3
  crop: true
  crop_margin: 8
  keep_structure: true

run:
  out_dir: .
